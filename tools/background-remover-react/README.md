# CodexPro Xóa Nền — React/Electron

Giao diện desktop React dùng BiRefNet AI chạy cục bộ để nhận diện chủ thể,
giữ chi tiết ảnh và xuất PNG alpha hoặc GIF động. GIF được xử lý riêng từng
frame và giữ nguyên thời lượng chuyển động.

```sh
npm install
npm run dev
npm run pack:mac
```
